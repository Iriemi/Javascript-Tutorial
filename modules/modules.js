// Example of using modules in JavaScript
// Modules are used to breakup larger codes into smaller codes in javascript to make it easier to read and debug

// A Normal export is import inside a curly bracket
import { aSimpleFunction, name, person, something } from './example1'

// A defaultexport is imported without any curly brackets
import anImportantFunction from './example1'